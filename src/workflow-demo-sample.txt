Pi 主会话演示样例（DEMO，不是当前项目的问题）

function mean(xs) {
  return xs.reduce((a, b) => a + b, 0) / xs.length;
}

正常输入：[2, 4, 6] => 4；[0] => 0。
边界输入：[] => NaN，需要由调用方明确空数组约定。
主会话的检查命令会特意返回退出码 1，以展示错误工具卡片和继续执行。
两个真实子代理分别分析边界输入与正常输入；本演示不写入用户文件。
